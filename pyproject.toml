[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "drkvl"
version = "2.0.0"
description = "vless vpn client for linux"
requires-python = ">=3.9"

[project.scripts]
drkvl = "drkvl.cli:main"

[tool.setuptools.packages.find]
include = ["drkvl*"]
